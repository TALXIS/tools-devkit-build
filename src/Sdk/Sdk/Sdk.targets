<Project>
  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" />

  <!-- Split by concern for readability - see each file's own top-of-file comment for details:
       - Sdk.PackageReference.targets: wires in the per-project-type Dataverse package.
       - Sdk.GitVersioning.targets: Git-based version-number defaults.
       - Sdk.NodeRestore.targets: restore-time Node dependency hydration anchor. -->
  <Import Project="Sdk.PackageReference.targets" />
  <Import Project="Sdk.GitVersioning.targets" />
  <Import Project="Sdk.NodeRestore.targets" />
</Project>
